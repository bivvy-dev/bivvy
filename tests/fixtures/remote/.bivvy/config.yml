app_name: remote-acceptance-tests

steps:
  acceptance-tests:
    title: "Run remote acceptance tests"
    command: "./run-acceptance-tests.sh"
    completed_check:
      type: marker

workflows:
  default:
    steps: [acceptance-tests]
