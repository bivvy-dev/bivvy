name: terraform-init
description: "Initialize Terraform and download providers"
category: iac
version: "1.0.0"
platforms: [macos, linux, windows]

detectors: [terraform-project.files]

step:
  title: "Initialize Terraform"
  description: "Initialize Terraform working directory and download providers"
  command: "terraform init"
  requires:
    - terraform
  checks:
    - type: presence
      target: .terraform
    - type: change
      target: "*.tf"
      on_change: proceed
    - type: change
      target: .terraform.lock.hcl
      on_change: proceed

environment_impact:
  note: "Providers and modules are downloaded to the .terraform directory"
