name: swift-resolve
description: "Resolve Swift package dependencies"
category: swift
version: "1.0.0"
platforms: [macos, linux]

detectors: [package-swift-present.file]

step:
  title: "Resolve Swift packages"
  description: "Download and resolve Swift Package Manager dependencies"
  command: "swift package resolve"
  checks:
    - type: presence
      target: .build
    - type: change
      target: Package.swift
      on_change: proceed
    - type: change
      target: Package.resolved
      on_change: proceed

environment_impact:
  note: "Dependencies are resolved into the .build directory"
