name: yum-install
description: "Install system packages using YUM"
category: system
version: "1.0.0"
platforms: [linux]

detectors: [yum-installed.command]

step:
  title: "Install system packages"
  description: "Install packages using YUM package manager"
  command: "sudo yum install -y"


environment_impact:
  note: "System packages are installed globally"
