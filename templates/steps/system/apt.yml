name: apt
description: "Install system packages using APT"
category: system
version: "1.0.0"
platforms: [linux]

detects:
  - command: "apt-get --version"

step:
  title: "Install system packages"
  description: "Install packages using APT package manager"
  command: "sudo apt-get install -y"

environment_impact:
  note: "System packages are installed globally"
