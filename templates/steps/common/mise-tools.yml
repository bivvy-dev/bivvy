name: mise-tools
description: "Install tool versions using mise"
category: version_manager
version: "1.0.0"
platforms: [macos, linux, windows]

detectors: [mise-configured.files]

step:
  title: "Install tool versions"
  description: "Install language runtimes and tools defined in mise config"
  command: "mise install"
  checks:
    - type: execution
      command: "mise current"
      validation: success
    - type: change
      target: .mise.toml
      on_change: proceed
    - type: change
      target: mise.toml
      on_change: proceed

environment_impact:
  modifies_path: true
  shell_files:
    - ~/.zshrc
    - ~/.bashrc
  note: "mise manages PATH shims for installed tools"
